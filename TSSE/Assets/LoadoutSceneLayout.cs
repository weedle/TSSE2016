using UnityEngine;
using System.Collections;

public class LoadoutSceneLayout : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //GameObject.Find("Text").transform.position =
        //    Camera.main.GetComponent<CameraHelper>().getRightMid()
           ;// + new Vector3(0,-1f,0);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
